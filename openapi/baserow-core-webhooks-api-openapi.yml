openapi: 3.2.0
info:
  title: Baserow API spec Core webhooks API
  version: 2.2.2
  description: 'For more information about our REST API, please visit this page.


    For more information about our deprecation policy, please visit this page.'
  contact:
    url: https://baserow.io/contact
  license:
    name: MIT
    url: https://github.com/baserow/baserow/blob/develop/LICENSE
tags:
- name: Core webhooks
paths:
  /api/webhooks/{webhook_uid}/:
    get:
      operationId: handle_core_webhook_request_get
      description: Receives and handles a webhook request.
      parameters:
      - in: path
        name: webhook_uid
        schema:
          type: string
          format: uuid
        required: true
      tags:
      - Core webhooks
      security:
      - UserSource JWT: []
      - JWT: []
      - {}
      responses:
        '204':
          description: No response body
        '404':
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Machine readable error indicating what went wrong.
                    enum:
                    - ERROR_CORE_HTTP_WEBHOOK_SERVICE_DOES_NOT_EXIST
                  detail:
                    oneOf:
                    - type: string
                      format: string
                      description: Human readable details about what went wrong.
                    - type: object
                      format: object
                      description: Machine readable object about what went wrong.
          description: ''
      summary: Handle core webhook request get
      x-summary-source: derived
    post:
      operationId: handle_core_webhook_request_post
      description: Receives and handles a webhook request.
      parameters:
      - in: path
        name: webhook_uid
        schema:
          type: string
          format: uuid
        required: true
      tags:
      - Core webhooks
      security:
      - UserSource JWT: []
      - JWT: []
      - {}
      responses:
        '204':
          description: No response body
        '404':
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Machine readable error indicating what went wrong.
                    enum:
                    - ERROR_CORE_HTTP_WEBHOOK_SERVICE_DOES_NOT_EXIST
                  detail:
                    oneOf:
                    - type: string
                      format: string
                      description: Human readable details about what went wrong.
                    - type: object
                      format: object
                      description: Machine readable object about what went wrong.
          description: ''
      summary: Handle core webhook request post
      x-summary-source: derived
    put:
      operationId: handle_core_webhook_request_put
      description: Receives and handles a webhook request.
      parameters:
      - in: path
        name: webhook_uid
        schema:
          type: string
          format: uuid
        required: true
      tags:
      - Core webhooks
      security:
      - UserSource JWT: []
      - JWT: []
      - {}
      responses:
        '204':
          description: No response body
        '404':
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Machine readable error indicating what went wrong.
                    enum:
                    - ERROR_CORE_HTTP_WEBHOOK_SERVICE_DOES_NOT_EXIST
                  detail:
                    oneOf:
                    - type: string
                      format: string
                      description: Human readable details about what went wrong.
                    - type: object
                      format: object
                      description: Machine readable object about what went wrong.
          description: ''
      summary: Handle core webhook request put
      x-summary-source: derived
    patch:
      operationId: handle_core_webhook_request_patch
      description: Receives and handles a webhook request.
      parameters:
      - in: path
        name: webhook_uid
        schema:
          type: string
          format: uuid
        required: true
      tags:
      - Core webhooks
      security:
      - UserSource JWT: []
      - JWT: []
      - {}
      responses:
        '204':
          description: No response body
        '404':
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Machine readable error indicating what went wrong.
                    enum:
                    - ERROR_CORE_HTTP_WEBHOOK_SERVICE_DOES_NOT_EXIST
                  detail:
                    oneOf:
                    - type: string
                      format: string
                      description: Human readable details about what went wrong.
                    - type: object
                      format: object
                      description: Machine readable object about what went wrong.
          description: ''
      summary: Handle core webhook request patch
      x-summary-source: derived
    delete:
      operationId: handle_core_webhook_request_delete
      description: Receives and handles a webhook request.
      parameters:
      - in: path
        name: webhook_uid
        schema:
          type: string
          format: uuid
        required: true
      tags:
      - Core webhooks
      security:
      - UserSource JWT: []
      - JWT: []
      - {}
      responses:
        '204':
          description: No response body
        '404':
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    description: Machine readable error indicating what went wrong.
                    enum:
                    - ERROR_CORE_HTTP_WEBHOOK_SERVICE_DOES_NOT_EXIST
                  detail:
                    oneOf:
                    - type: string
                      format: string
                      description: Human readable details about what went wrong.
                    - type: object
                      format: object
                      description: Machine readable object about what went wrong.
          description: ''
      summary: Handle core webhook request delete
      x-summary-source: derived
components:
  securitySchemes:
    Database_token:
      type: http
      scheme: bearer
      bearerFormat: Token your_token
    JWT:
      type: http
      scheme: bearer
      bearerFormat: JWT your_token
    UserSource_JWT:
      type: http
      scheme: bearer
      bearerFormat: JWT your_token
